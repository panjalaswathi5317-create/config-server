server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/leavemanagement?useSSL=false&serverTimezone=UTC
    username: root
    password: rootroot
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

app:
  jwt:
    secret: "change-this-secret-to-a-long-random-string"
